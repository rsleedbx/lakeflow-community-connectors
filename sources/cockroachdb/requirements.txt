pg8000>=1.30.0

# Azure Parquet Mode dependencies (installed automatically if Azure credentials provided)
azure-storage-blob>=12.0.0
pyarrow>=10.0.0
pandas>=1.5.0

# Databricks SDK for sync scripts
databricks-sdk>=0.18.0

# Rich for beautiful CLI output (optional, for sync_azure_to_volume_compact.py)
rich>=13.0.0

# 
python-dotenv
GitPython