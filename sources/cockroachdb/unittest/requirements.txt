# Unit Test Dependencies for CockroachDB Connector
# Install with: pip install -r requirements.txt

# Git repository operations (required for finding git root)
GitPython>=3.1.0

# Environment variable management
python-dotenv>=1.0.0

# CockroachDB PostgreSQL driver
pg8000>=1.30.0

# Optional: Jupyter for running notebooks locally
jupyter>=1.0.0
notebook>=6.0.0







